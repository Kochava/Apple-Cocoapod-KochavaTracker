use_frameworks!

platform :ios, '10.3'

target 'Apple-Cocoapod-KochavaTracker_Example' do
  pod 'Apple-Cocoapod-KochavaTracker', :path => '../'

  target 'Apple-Cocoapod-KochavaTracker_Tests' do
    inherit! :search_paths

    
  end
end
